language: php
php:
  - 7.1

before_script:
  - composer self-update
  - composer install

script:
  - vendor/bin/phpunit --filter="DescribeRegion"

branches:
  only:
  - master
  - "/^v?[0-9\\.]+/"

notifications:
  email: false
